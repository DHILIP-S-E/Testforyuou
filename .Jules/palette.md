## 2025-02-07 - Delete Confirmation Pattern
**Learning:** The application relied on `window.confirm` for destructive actions, which is poor UX.
**Action:** Replace `window.confirm` with a custom `Dialog` using `MUI` components for better accessibility and visual consistency.
