import React from 'react';
import { Box, Typography } from '@mui/material';

export const ChatPage: React.FC = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Chat App</Typography>
      <Typography>This is a placeholder for the Chat module.</Typography>
    </Box>
  );
};
