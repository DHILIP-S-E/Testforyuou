import React from 'react';
import { Box, Typography } from '@mui/material';

export const CalendarPage: React.FC = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Calendar App</Typography>
      <Typography>This is a placeholder for the Calendar module.</Typography>
    </Box>
  );
};
